# Published release tag, or latest.
AUTO_ROUTER_VERSION=latest

# Keep the admin UI local by default. Put a TLS reverse proxy in front before
# binding to 0.0.0.0 or exposing this port to an untrusted network.
AUTO_ROUTER_BIND=127.0.0.1
AUTO_ROUTER_PORT=8080
